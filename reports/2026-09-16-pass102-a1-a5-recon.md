# Pass 102 — sorting inventory items A1–A5 for building

**Date:** 2026-09-16
**READ-ONLY.** No source file, test file, project file or asset was changed. No build, no device
run, neither Apple TV touched. `~/Xcode/marlin-dvr-reference` was not fetched, pulled or checked
out. **Every request to `http://192.168.1.250:8090/` was a GET on a read-only route**; no POST, PUT,
PATCH or DELETE was sent, and `GET /api/settings` was not read (the notebook forbids it).

**HEAD every `file:line` below was read at: `90ff524644ec332797736b70840f8b8671304f14`** (Pass 101).
Line numbers drift and are never rewritten afterwards (COLD-START, *Standing state of the devices*).

**Nothing here is built, proposed or ranked beyond the sort the pass asked for.** Where the notebook
records no option, this report says so rather than inventing one.

---

## 0. The three versions, and what each route claim is worth

| Thing | Version | Read how |
|---|---|---|
| The running server | **1.9.1** | `GET /api/status` today → `{"name":"marlin-dvr","version":"1.9.1","uptime_seconds":1067,"port":8089}` |
| The reference clone `~/Xcode/marlin-dvr-reference` | **1.8.1** | `HEAD` = `eb0c098de3efc8bb569e252b63e141aa10262918`, `cmd/marlin-dvr/main.go:38` `appVersion = "1.8.1"` |
| The contract `HLS-CLIENT-API.md` in that clone | **1.7.0** | its own header, lines 5–8 |

**The clone is two releases behind the running server** (1.8.1 → 1.8.2 → 1.9.1) and the contract is
three. COLD-START already records the contract lag (*The server*, Pass 41). So every route claim
below is labelled:

- **[GET]** — confirmed against the running **1.9.1** server today, by a GET that returned 200.
- **[source]** — read from the clone's Go at **1.8.1**. Never called by this pass.
- **[contract]** — read from `HLS-CLIENT-API.md` at **1.7.0**. Never called by this pass.

**What could not be confirmed:** every **write** route A1–A5 needs. They are write routes, so this
pass never called them, and the newest source it can read is 1.8.1 — two releases behind. Nothing
in Pass 101's relayed 1.9.1 notes touches `POST /api/passes`, `PUT /api/library/recordings/{id}` or
`PUT /api/schedule/jobs/{id}` (that change was the `.mp4` sidecar on `format:"file"`), but **this
pass did not verify that and a build pass should not assume it.** The read routes carry no such
doubt: all seven were answered 200 by 1.9.1 itself, today.

### Read routes, confirmed by GET on 1.9.1 today

| Route | HTTP | Top-level keys |
|---|---|---|
| `GET /api/status` | 200 | `name, version, uptime_seconds, port` |
| `GET /api/system` | 200 | `version, build, hostname, uptimeSeconds, uptime, startedAt, timezone, localTime, board, distro, kernel, cpu` |
| `GET /api/schedule` | 200 | `count, groups, passes` |
| `GET /api/passes` | 200 | `count, jobs, passes` |
| `GET /api/library/trash` | 200 | `count, recordings` |
| `GET /api/guide/now` | 200 | `at, count, programs` |
| `GET /api/channels` | 200 | `channels, count, sources` |

### The server's state while this recon ran, because two items turn on it

- **`GET /api/schedule`: 8 jobs in 3 groups (Today, Monday, Tuesday). Every one is `Queued`, every
  one belongs to a pass, none is `manual`, and none is `Skipped`.**
- **`GET /api/passes`: 8 passes. Every one has `seriesId` of the form `title:<lowercased title>`
  and `channel: ""`.**
- **`GET /api/guide/now`: 88 programmes, and all 88 carry a `title:`-prefixed `seriesId`** (the
  nested `program.seriesId`; the row object has none).
- `GET /api/library`: 11 recordings.

---

## 1. Each item: the design, the code now, the routes

### A1 — show detail's "Series pass" does nothing

**Design.** Frame **5d**, labelled *"Show detail — episodes newest first, app-side resume"*. The
button block under the title is, in order, a primary `Resume S9 E11 · 22 min in`, then a row of two:
`Play newest` and **`Series pass`**. That is the whole of it — **the design gives the button a label
and a position and names no behaviour**: no confirmation, no state when a pass already exists, no
copy for the result.

**Code now.**

- `ShowDetailScreen.swift:195` — `inert("Series pass", id: "pass")`, inside the `HStack` that also
  holds the working `Play newest` button (`:186-194`).
- `ShowDetailScreen.swift:208-216` — the `inert(_:id:)` helper: a `Button` whose action body is the
  comment `// "Series pass" here is not in Pass 8's steps (the airing sheet's is); it stays inert.`
- `ShowDetailScreen.swift:13-14` — the file header says the same: *"'Series pass' stays inert: this
  pass wires the airing sheet's series pass only (Pass 8 scope lock)."*

**The call already exists and is already used.** `ServerWrites.swift:192` `createPass(title:seriesId:)`,
called today from `AiringSheet.swift:376`.

**The screen already knows whether the show has a pass, with no new read.** `ShowResponse.pass`
(`Models.swift:407`) is documented in the app as *"the matching pass title or `""`"*, and the server
fills it by a case-insensitive title match over every pass — `library.go:659-664` **[source]**:

```go
var passTitle string
for _, p := range a.listPasses() {
    if strings.EqualFold(p.Title, title) {
        passTitle = p.Title
    }
}
```

`ShowDetailModel.load()` already fetches `GET /api/library/shows/{id}` (`ShowDetailScreen.swift:40`),
so the answer is in hand before the button is drawn.

**Routes needed.**

| Route | Kind | Confirmed |
|---|---|---|
| `POST /api/passes` | **write** | **[source]** `passes.go:705-737`; **[contract]** not described (the contract covers play sessions only) |
| `GET /api/library/shows/{id}` | read | already made by this screen; not separately re-GET by this pass |

**What the server does with it** — `passes.go:718-725` **[source]**: a request with no `seriesId`
gets `p.SeriesID = "title:" + strings.ToLower(p.Title)`, then a **409** `a pass for this series
already exists: <title>` when an existing pass has the same `SeriesID` **and** the same `Channel`.

**The series-id worry is measured away on this server.** `Pass.matches` (`passes.go:211-231`
**[source]**) compares series ids **only when both are non-empty and neither is `title:`-prefixed**;
otherwise it falls through to `strings.EqualFold` on the titles. Since **all 88 programmes and all 8
passes on this server carry `title:`-prefixed ids** (§0), a pass created from show detail with no
`seriesId` matches exactly the airings a sheet-created pass would, and collides with an existing one
exactly as it should. Three of the four library show titles read today match a pass title verbatim
("History's Greatest Mysteries", "The Proof Is Out There", "The Food That Built America"), so the
409 path is not hypothetical here.

### A2 — the Player's "Delete this recording" (frame 6e)

**Design.** Frame **6e**, held in the design's `playerMessages` data rather than as a drawn artboard:

```
{id:"6e", label:"Ended — recording finished", code:"Marked watched on the server",
 title:"Home Town", sub:"S9 E12 · Bright Spot on Bay Street",
 body:"Next episode starts in 8 seconds.",
 btns:["Play S9 E13","Back to the show","Delete this recording"]}
```

Three buttons, the third **"Delete this recording"**. As with A1, the design gives a label and no
behaviour — no confirm step, no copy for what happens next, no statement of where the viewer lands.

**Code now — and a correction to the inventory.** Item A2 is written in
`reports/2026-09-16-pass99-unfinished-inventory.md` as *"The Player's **'Delete this recording'**
(frame 6e) is absent."* **It is not absent. It is drawn and inert:**

- `PlayerScreen.swift:393` —
  `StateButton(title: "Delete this recording", id: "delete", primary: false, focused: focused) { }`
  — an **empty action closure**, with `PlayerScreen.swift:392` saying
  `// Not in Pass 8's steps (step 3 is the show-detail long-press menu); still inert.`

COLD-START's own sentence is the accurate one — *"Deliberately still inert **or** absent"* — so the
notebook is right and only the inventory row's wording is loose. Nothing else in A2 changes.

**Where 6e is reached from, which narrows the item usefully.** `PlayerScreen.swift:49` draws
`EndedState` for `.ended`; `.ended` is set in exactly one place, `PlayerModel.swift:722`, inside
`playedToEnd()` whose first line is `guard isRecording, phase == .playing, let episode = request.episode`
(`PlayerModel.swift:709`). **Frame 6e is therefore reached only by a recording played to its end** —
never live, never a camera. The button can never appear where it makes no sense.

**The id is already in hand.** `PlayRequest.recording(episode:show:start:)` (`PlayRequest.swift:13`)
carries the `Episode`, and `updateRecording(id:flag:)` wants exactly `episode.id`. No new read.

**Routes needed.**

| Route | Kind | Confirmed |
|---|---|---|
| `PUT /api/library/recordings/{id}` body `{"trash": true}` | **write** | **[source]** `library.go:650-691`; the app's own call is `ServerWrites.swift:200-209` |

**Why this one is destructive and not merely a flag.** `ServerWrites.swift:95-97` records it in the
app's own words: the route *"answers the updated `episodeView`, **or `{ok, deleted}` when 'Remove
Items From Trash After' is 'Immediately' and the trash flag removed the file at once**
(library.go:683-691)"*. `RecordingUpdate` (`ServerWrites.swift:98-116`) decodes both. **Which of the
two the owner's server gives is not known to this pass**: reading it means `GET /api/settings`, which
the notebook forbids. Under the "Immediately" setting the button deletes the file outright.

**And the screens behind it would not know.** The Player is a `fullScreenCover` at the app root —
`ContentView.swift:53-56`, with `.interactiveDismissDisabled(true)` at `:59` — and the
`onLibraryChanged` callback that exists for exactly this problem runs only ShowDetail → Recordings
(`ShowDetailScreen.swift:85`, `:118`; `RecordingsScreen.swift:166`). **Nothing connects the Player to
it.** This is the same staleness COLD-START records for Continue watching (Pass 91).

### A3 — a plain click on the Guide's channel cell does nothing

**Design.** Frame **3a**, *"Comfortable — 8 rows, 2-hour window"*. The left column is 300 pt wide and
holds an initials tile, the channel name and the channel number — that is all the frame contains for
it. **The design names no interaction for the channel cell at all**: no click, no hold, no menu. The
hold-to-favourite is Pass 9's addition, not the design's. The design's only "tune this" control is
on frame **5c** (*"Airing detail over the guide — Record Now and Series Pass"*), whose third button
is **`Watch live`**; frame 3a's own footer says only *"Starts at the current half hour · forward
only"*.

**Code now.**

- `GuideScreen.swift:769` — `HoldButton(hold: hold) { }` wrapping the `ChannelCell`, with an **empty
  action closure**, and `:767-768` saying *"the channel cell takes focus so a click-and-hold can
  favourite the channel. **A plain click is not in Pass 9's steps and does nothing.**"*
- The hold path: `GuideScreen.swift:460` `.onChange(of: hold.holds)` → `handleHold()` (`:332-349`) →
  `channelMenu = row.channel` → `ChannelActionsMenu`, whose **only** row is Favorite / Unfavorite
  (`ChannelActionsMenu.swift:48-56`).

**The behaviour Pass 9's question asks about already exists one column to the right.**
`GuideScreen.swift:321-330`:

```swift
/// DECISIONS.md 2026-09-06: a click on a program airing now plays it; a click on a
/// future airing opens the sheet; press-and-hold on a current cell opens the sheet.
private func select(_ cell: GuideCellItem) {
    let now = Int(Date().timeIntervalSince1970)
    if cell.program.start <= now && now < cell.program.end {
        onPlay(.live(channel: cell.channel, program: cell.program))
    } else { … }
}
```

So "tune this channel live" is already a click on the **now** programme cell of that row.

**Routes needed: none that is new.** `PlayRequest.live(channel:program:)` (`PlayRequest.swift:13`)
resolves to `POST /api/play/sessions` with `kind: "live"` (`:28`) and `format: "hls"` (`:48`) —
**[contract]** `HLS-CLIENT-API.md:59` *"One route for all three kinds: `POST /api/play/sessions`"*
and `:66` for the `kind` field. This is the route the Guide already drives today.

**The click/hold collision is already solved.** `HoldButton`'s action is guarded by
`hold.shouldSwallowClick()` (`RemoteHold.swift:173-175`), and `armSwallow()` / `pressEnded()` /
`shouldSwallowClick()` (`RemoteHold.swift:66-82`) are the same mechanism every programme cell's click
already passes through.

### A4 — a cancelled pass airing cannot be un-skipped from the app

**Design.** **Manage DVR is not in the approved design.** The string "Manage" appears **0 times** in
`design/Marlin DVR TV.dc.html`, and the app says so itself — `ManageDVRScreen.swift:5-6`: *"the
management area the owner's old DVR had … **Not in the approved design — built to the app's own
look.**"* There is nothing to quote, and nothing a build pass must match.

**Code now.**

- `ServerWrites.swift:249` — `cancelJob(id:)` sends `["skipped": true]` and prints the result. **No
  call anywhere sends `false`** (`grep` over the app target: the only `skipped` write is this one).
- `ScheduleManageView.swift:292-296` — `ScheduledJobDetail`'s only destructive row, "Cancel
  recording", armed on the first click and sent on the second.
- **The list does not filter by status** — `ScheduleManageView.swift:127-132` draws a `Button` for
  every `job` in every group, so a `Skipped` airing is already drawn and already opens the detail,
  which already shows its `JobStatusChip(status: job.status)` (`:270`).
- **The fields are already decoded**: `Job.status` and `Job.reason` at `Models.swift:295-296`.

**Two branches a build must respect, both read from source.** `passes.go:927-967` **[source]**:

1. **A one-off Record Now is removed, not skipped.** With `passId == "manual"`, `skipped:true`
   deletes the job from `a.jobsState.Manual` and answers `removed: true` (`passes.go:938-952`).
   **There is nothing left to un-skip** — the airing is gone from the schedule entirely. The app
   already tells these apart (`ScheduleManageView.swift:243` `isManual`, and the copy at `:245-252`).
2. **An airing skipped by the pass limit is recomputed every build.** `passes.go:443-450` sets
   `Status = "Skipped"` with `Reason = "over the pass limit of N"` while assembling the schedule, not
   from stored state — so `{"skipped": false}` **would not stick**. `Job.reason` already distinguishes
   it: an owner-cancelled airing has no reason string.

Only the third case — a pass airing the owner cancelled, held in `a.jobsState.Skipped` — is
un-skippable, by `passes.go:954` `delete(a.jobsState.Skipped, id)`.

**A wart this recon found, recorded not fixed:** a `Skipped` pass airing today still shows
**"Cancel recording"**, which would send `skipped:true` a second time — on the server a no-op
(`a.jobsState.Skipped[id] = true` when it is already true, `passes.go:951`).

**Routes needed.**

| Route | Kind | Confirmed |
|---|---|---|
| `PUT /api/schedule/jobs/{id}` body `{"skipped": false}` | **write** | **[source]** `passes.go:927-967`, the `else` branch at `:953-955` |
| `GET /api/schedule` | read | **[GET]** 200 today |

### A5 — the Manage DVR lists never refresh on their own

**Design.** Not in the approved design, exactly as A4.

**Code now.**

- `ManageDVRScreen.swift:127-128` — `.task { await model.load() }`. **One read on open**, and that is
  the only unprompted read on the screen.
- `ManageDVRScreen.swift:55-68` — `load()` runs `GET /api/system`, `GET /api/schedule` and
  `GET /api/passes` concurrently, then `refreshTrash()`.
- The three refresh methods exist — `refreshSchedule()` `:70`, `refreshPasses()` `:74`,
  `refreshTrash()` `:79` — and **every caller is a write's completion**:
  `ScheduleManageView.swift:50`, `:68`, `:75-76`; `PassesManageView.swift:31`, `:37-38`;
  `TrashManageView.swift:138`, `:161`. **Nothing is on a timer.**

**The app already contains the mechanism, proven.** The Guide's once-a-minute beat is a `.task` on
that screen and nothing else — `GuideScreen.swift:440-452`, which sleeps to the next minute, calls
`beat()`, counts the beats and prints `[guide] clock stopped after N beat(s)` when the screen goes
(Passes 79, 80). `GuideScreen.swift:440-444` states the reason it is a `.task`: *"no beat can outlive
the screen as an orphan."*

**Routes needed — all reads, all confirmed today.**

| Route | Kind | Confirmed |
|---|---|---|
| `GET /api/system` | read | **[GET]** 200 |
| `GET /api/schedule` | read | **[GET]** 200 |
| `GET /api/passes` | read | **[GET]** 200 |
| `GET /api/library/trash` | read | **[GET]** 200 |

---

## 2. Route confirmation, in one table

| Item | Route | Kind | How confirmed |
|---|---|---|---|
| A1 | `POST /api/passes` | write | **[source]** `passes.go:705-737` — **not confirmable at 1.9.1**, never called |
| A1 | `GET /api/library/shows/{id}` | read | already made by the screen; the `pass` field's rule is **[source]** `library.go:659-664` |
| A2 | `PUT /api/library/recordings/{id}` `{"trash": true}` | write | **[source]** `library.go:650-691` — **not confirmable at 1.9.1**, never called |
| A3 | `POST /api/play/sessions` `kind:"live"` | write | **[contract]** `HLS-CLIENT-API.md:59,66` — the route the Guide already drives; never called by this pass |
| A4 | `PUT /api/schedule/jobs/{id}` `{"skipped": false}` | write | **[source]** `passes.go:953-955` — **not confirmable at 1.9.1**, never called |
| A4 | `GET /api/schedule` | read | **[GET]** 200 on 1.9.1 today |
| A5 | `GET /api/system`, `/api/schedule`, `/api/passes`, `/api/library/trash` | read | **[GET]** 200 on 1.9.1 today |

**No write route was called. Not one.**

---

## 3. Where a build needs the owner's call

Each with the file it lives in, the quote, and **only** the options the notebook already records.

**3.1 — A3 is blocked outright. Pass 9 Open Question 1**
(`reports/2026-09-06-pass9-sweep4-fixes.md:290-291`):

> **A plain click on a channel cell does nothing.** The cell had to become focusable for the hold;
> no click behaviour was named, so it has none. **Should a click tune the channel live?**

**Options the notebook records: exactly one** — tune the channel live. There is no second candidate
anywhere in COLD-START, DECISIONS or the reports, and none is invented here. **A3 cannot be built
until this is answered**, because the item is the question. Two facts the owner may want in front of
him when he answers, both measured above and neither an option: the design names no interaction for
that cell at all, and a click on the row's **now** programme cell already tunes it live
(`GuideScreen.swift:321-330`).

**3.2 — A2's destructiveness. `ServerWrites.swift:95-97`:**

> `PUT /api/library/recordings/{id}` answers the updated `episodeView`, **or `{ok, deleted}` when
> "Remove Items From Trash After" is "Immediately" and the trash flag removed the file at once**
> (library.go:683-691).

The owner's server's setting **was not read** — `GET /api/settings` is off limits. Whether 6e's
button moves a recording to the trash or deletes it outright therefore depends on a value this
project has chosen not to read. **His call**, and the notebook records no options for it.

**3.3 — A2's confirm step.** The design gives frame 6e three buttons and **no confirm**
(`playerMessages`, quoted in §1). Every other destructive control the app has is armed on the first
click and sent on the second — `ScheduleManageView.swift:292` ("Cancel recording — click again to
confirm"), the Empty Trash guard recorded in COLD-START, `AiringSheet.swift:277` ("Stop recording —
click again"). **The notebook records no decision for 6e.** Matching the design means no confirm;
matching the app's own habit means a confirm. His call.

**3.4 — A4's scope. Pass 10 Open Question 4**
(`reports/2026-09-06-pass10-favorites-and-manage.md:262-264`):

> **A skipped pass airing cannot be un-skipped from the app.** The server takes `{"skipped": false}`
> (passes.go:955) and the app never sends it, so a cancelled pass airing can only be restored from
> the web UI.

**Options the notebook records: none** — it records the absence and the route, not a choice of
design. What a build pass needs decided is what the two dead branches of §1/A4 should *say*: a
removed one-off Record Now and a limit-skipped airing both look like "Skipped" in the list and
neither can be restored by this call. The notebook offers no copy for either.

**3.5 — A5's interval and its cost. Pass 10 Open Question 2**
(`reports/2026-09-06-pass10-favorites-and-manage.md:255-257`):

> **Nothing auto-refreshes.** The lists read when they open and after a write, per the scope lock. A
> recording that starts while the schedule list is open still reads "Queued" until the screen is
> re-entered.

**Options the notebook records: none for Manage DVR.** The nearest recorded question is Pass 8 Open
Question 5 (`reports/2026-09-06-pass8-sweep4-writes.md:356-359`), and it is about the **Guide**, not
Manage DVR: *"Refresh the schedule on a timer, or on every window move?"* The only implemented
precedent in the app is the Guide's **once a minute** beat (`GuideScreen.swift:445-452`). An interval
for Manage DVR has never been named by anyone.

**3.6 — A1 has no recorded open question of its own** beyond Pass 8 Open Question 1's *"Wire them in
a follow-up?"*, which the owner has now answered by ordering A1–A5 built. Two decisions the notebook
does not settle: what the button should read and do when `ShowResponse.pass` is non-empty (the show
already has a pass — the airing sheet's answer is to swap the control to "Edit series pass",
`AiringSheet.swift:260-265`, but nothing says show detail should), and whether a pass created here
should warn about `recordMode: "new"` the way Pass 8 Open Question 3 asked for the sheet
(`reports/2026-09-06-pass8-sweep4-writes.md:349-352`). Neither is invented here; both are named
because a build pass will hit them on its first screen.

---

## 4. The sort

### SWEEP — additive, independent, clear of do-not-touch

| Item | One-line reason |
|---|---|
| **A1** | One call to `createPass`, which already exists and is already used; the screen already holds the answer it needs in `ShowResponse.pass`; nothing else on the screen changes. |
| **A3** | One closure at `GuideScreen.swift:769`, reusing `PlayRequest.live` and a click/hold guard already proven on every programme cell — **but it cannot start until 3.1 is answered.** |
| **A4** | A second `MenuRow` beside "Cancel recording" and one write that *restores* rather than removes; status and reason are already decoded and skipped rows are already drawn and clickable. |
| **A5** | A `.task` beat copied from a mechanism this app already runs once a minute in the Guide, driving four reads that were all confirmed 200 today; no write, no new route. |

### STANDALONE — destructive, fragile, or plainly hard

| Item | One-line reason |
|---|---|
| **A2** | The one destructive item: the same call **deletes the file outright** when the server's trash period is "Immediately" (`ServerWrites.swift:95-97`), and it fires inside the Player's end-of-recording teardown, after `playedToEnd()` has already stopped the session and marked it watched, on a root-level `fullScreenCover` with `.interactiveDismissDisabled(true)` and no library-change callback. |

**Nothing was added to any item.** The sort is four in, one out; A3 is in the sweep but gated on an
answer, and that gate is stated rather than designed around.

---

## 5. What a build pass must prove on Home Theater, and what can only be traced

### A1
- **Prove on the device:** press "Series pass" on a show with **no** pass and see the pass appear —
  the server's own `passView` count in the console line `ServerWrites.swift:195` writes, and the new
  pass in Manage DVR → Your Passes. **Then press it on a show that already has one** and see the
  **409** path (`passes.go:721-725`) drawn as something other than a silent failure; three of the
  four library shows read today already have a matching pass, so this costs no setup.
- **Cost, and it is a real write:** creating a pass **queues recordings**. The owner has 8 passes and
  8 queued jobs now. A pass created for evidence should be one he wants, or deleted afterwards by
  `deletePass` (`ServerWrites.swift:242`) — **his call, and this pass does not assume it**.
- **Traceable only:** nothing of substance. This item is fully provable on the device.

### A2
- **Prove on the device:** frame 6e itself, which needs a **recording played to its end**
  (`PlayerModel.swift:709`) — the harness `CommercialSkipUITests` already knows how long that takes,
  and COLD-START records that a stale resume position deep inside *History's Greatest Mysteries*
  S4 E14 makes that harness fail until it is cleared. Then the button: the write's console line
  (`ServerWrites.swift:202-208`), which of `.episode` / `.deleted` came back, and where focus and the
  screen land afterwards.
- **Can only be traced, or needs the owner first:** whether the recording is recoverable. That is the
  "Immediately" setting (3.2), unreadable here. **A device run that presses this button destroys one
  of the owner's 11 recordings** — reversibly or not, unknown until he says. No such run should
  happen without him naming the recording.
- **Traceable only:** the staleness of Recordings and show detail behind the Player, unless the build
  also wires a callback — and wiring one is **not** in A2 as the inventory states it.

### A3
- **Prove on the device:** a click on a channel cell tunes that channel — picture, and the session in
  the server's log — **and, in the same run, that a click-and-hold still favourites and does not also
  tune** (`RemoteHold.swift:66-82`; `RemoteHoldUITests` is the existing harness, Pass 9).
- **Traceable only:** nothing. This is a two-gesture item on one control and both gestures are
  physical; the simulator cannot settle it, as Pass 9 already found.

### A4
- **Prove on the device:** the round trip — cancel a pass airing, see it read `Skipped`, un-skip it,
  see it read `Queued` again, and see the server's `{"ok":true,"skipped":false,"removed":false}`.
- **The precondition is the problem, and it is the owner's to authorise.** There is **no skipped
  airing on his server right now** — all 8 jobs are `Queued` (§0). Producing one means **cancelling a
  real scheduled recording of his**, which is precisely what Pass 10 declined to do: *"the only pass
  airing on the server is the owner's and skipping it would have changed his schedule"*
  (`reports/2026-09-06-pass10-favorites-and-manage.md:258-261`). A build pass must either get that
  authorisation by name or leave the round trip unproven.
- **Traceable only:** the two dead branches — a removed one-off Record Now, and a limit-skipped
  airing whose un-skip would not stick (`passes.go:443-450`). Neither can be produced without more
  writes to his schedule, and the second cannot be produced at all without a pass with a `Limit`.

### A5
- **Prove on the device:** that a list changes **under the viewer's eyes** without the screen being
  left — the honest subject is the one Pass 10 named, a recording that starts while the schedule list
  is open turning from `Queued` to `Recording`. That needs an airing to start during the run, so it
  is clock-bound, not press-bound.
- **Prove on the device, and this is the risk worth the run:** **that a refresh under focus does not
  move focus.** The rows are a `ForEach` over `group.items` with `@FocusState` on `job.id`
  (`ScheduleManageView.swift:127-132`); replacing the array while a row is focused is exactly the
  class of defect Passes 77–80 spent two passes on in the Guide.
- **Prove on the device:** that the beat **stops** when the screen does — the Guide prints
  `[guide] clock stopped after N beat(s)` for this reason (`GuideScreen.swift:452`), and an orphaned
  beat polling four routes forever is the failure to look for.
- **Traceable only:** the empty-list and error branches, unless the server is made to fail, which is
  out of scope.

---

## 6. Open questions

1. **A3 is unanswerable by this project.** Pass 9 Open Question 1 is the item (3.1). Until the owner
   answers it, A3 cannot be built, and this recon deliberately records one option because one is all
   the notebook holds.
2. **The write routes are unconfirmed at 1.9.1** (§0). Should a build pass confirm each one against
   the running server before wiring it — which means calling a write — or take the 1.8.1 source?
   Nothing was called here.
3. **A2's trash period** (3.2) is a setting this project has ruled it will not read. Left to him.
4. **A4's precondition** (§5) needs the owner to name an airing he is willing to lose from his
   schedule for the length of a test, or the round trip stays unproven.
5. **Does A1's button become "Edit series pass" when a pass exists**, as the airing sheet's does
   (3.6)? Not settled anywhere.
6. **The skipped-row wart** (§1/A4) — "Cancel recording" offered on an already-`Skipped` airing —
   was found by this recon and is **not** part of A1–A5. Recorded, not built, not scheduled.

## 7. What I am least sure of

1. **That `POST /api/passes`, `PUT /api/library/recordings/{id}` and `PUT /api/schedule/jobs/{id}`
   behave at 1.9.1 the way the 1.8.1 source says.** Two releases separate the clone from the running
   server. The app exercises the first two in normal use, so a silent break is unlikely — but
   "unlikely" is not a measurement, and this pass took none.
2. **That A5's refresh will not fight focus.** I am confident the Guide's beat is the right
   precedent and that the mechanism transfers; I am not confident that swapping `group.items`
   under a focused row leaves focus where it was. The Guide needed two passes to get its own
   equivalent right, and Manage DVR's rows are identified by `job.id` from a list the server
   recomputes on every call.
3. **That A2 really is one call.** The call is one line. What is around it — dismissing a
   `fullScreenCover` that disables interactive dismissal, from a state reached after the session has
   already been torn down, and leaving two screens behind it showing a library that no longer
   matches — is not, and the inventory's one-line framing of A2 understates it. That is why it is
   sorted STANDALONE, but I may still be underestimating it.
